<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kgecproject.practice.demo.calendardemo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Main"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="kgecproject.practice.demo.calendardemo.Estimate">
            <!-- Added second activity: Estimates -->
        </activity>

        <activity android:name="kgecproject.practice.demo.calendardemo.DateCal">
            <!-- Added third activity: DateCal -->
        </activity>

        <activity android:name="kgecproject.practice.demo.calendardemo.DailyExpense">
            <!-- Added fourth activity: DailyExpense -->
        </activity>
    </application>

</manifest>